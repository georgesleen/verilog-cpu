# Core

CPU microarchitecture blocks belong here: fetch/decode/execute pipeline stages, register file, ALU/MUL/DIV, branch unit, CSR, and control logic. Keep interfaces clean so the top-level SoC can drop in new cores or variants without reshuffling the rest of the design.
