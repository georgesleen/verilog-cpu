# Peripherals

Lightweight peripherals that surround the core: UART, GPIO, timers, watchdogs, interrupt controller, and any board-facing IP. Keep register maps and address offsets documented in-line and align with the linker script used by `sw/`.
